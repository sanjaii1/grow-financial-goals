
export const useDownloadReport = (options: any) => {
    const handleDownloadPdf = () => {
        console.warn("Download functionality has been removed.");
    };
    const handleDownloadJson = (data: any) => {
        console.warn("Download functionality has been removed.");
    };
    return { handleDownloadPdf, handleDownloadJson };
};
